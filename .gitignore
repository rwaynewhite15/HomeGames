__pycache__/
*.pyc
.DS_Store
Thumbs.db
.claude/

# Live game data — the Pi runs the server, so its copies are the real ones.
# Tracking these meant every machine's play collided on the same two files.
stats.json
ai_profiles.json
chess_brains/
chess_ai.json
chess_ai_demo.json
*.tmp
